# Orchestrator Start Workflow
name: orchestrator-start
description: "프로젝트 초기화부터 자동 진행 - workflow-init 실행 후 전체 라이프사이클 자동 관리"
author: "ownuun"

# Configuration
config_source: "{project-root}/.bmad/bmm/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"

# Paths
installed_path: "{project-root}/.bmad/bmm/workflows/orchestrator"
workflow_status_file: "{output_folder}/bmm-workflow-status.yaml"

# Output configuration
template: false
instructions: "{installed_path}/start-instructions.md"

standalone: true
